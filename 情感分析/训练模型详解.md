# 训练后的模型详解

## 📁 模型文件结构

训练完成后，模型被保存在 `my_sentiment_model/` 文件夹中，包含以下文件：

```
my_sentiment_model/
├── config.json              # 模型配置文件
├── model.safetensors        # 模型权重文件（新格式）
├── tokenizer_config.json    # 分词器配置
├── tokenizer.json           # 分词器词汇表和规则
└── training_args.bin        # 训练参数（可选）
```

---

## 📄 各文件详解

### 1. `config.json` - 模型配置文件

这是模型的"设计图纸"，定义了模型的架构和参数。

```json
{
  "architectures": ["BertForSequenceClassification"],  // 模型类型
  "model_type": "bert",                                // 基础模型
  "hidden_size": 768,                                  // 隐藏层大小
  "num_hidden_layers": 12,                             // Transformer 层数
  "num_attention_heads": 12,                           // 注意力头数
  "intermediate_size": 3072,                           // 前馈网络大小
  "max_position_embeddings": 512,                      // 最大序列长度
  "vocab_size": 21128,                                 // 词汇表大小
  "num_labels": 2,                                     // 分类类别数
  "problem_type": "single_label_classification"        // 任务类型
}
```

**关键参数说明：**

| 参数 | 值 | 含义 |
|------|-----|------|
| `hidden_size` | 768 | 每个词的向量维度 |
| `num_hidden_layers` | 12 | BERT 有 12 层 Transformer |
| `num_attention_heads` | 12 | 每层有 12 个注意力头 |
| `vocab_size` | 21128 | 中文 BERT 的词汇量 |
| `max_position_embeddings` | 512 | 最多处理 512 个词 |

**模型参数量计算：**
- 总参数：约 102,269,186 个（102M）
- 包括：词嵌入、位置编码、12 层 Transformer、分类头

---

### 2. `model.safetensors` - 模型权重文件

这是模型的"大脑"，包含所有训练好的参数（权重）。

**文件格式：**
- **SafeTensors**: 新的安全格式（推荐）
  - 更快的加载速度
  - 更安全（防止恶意代码）
  - 更小的文件大小
  
- **旧格式**: `pytorch_model.bin`（PyTorch 格式）

**文件大小：**
- 约 400-500 MB（取决于模型大小）
- BERT-base: ~400MB
- BERT-large: ~1.3GB

**包含的权重：**
```
bert.embeddings.word_embeddings.weight        # 词嵌入
bert.embeddings.position_embeddings.weight    # 位置编码
bert.encoder.layer.0.attention.self.query     # 注意力层
bert.encoder.layer.0.attention.self.key
bert.encoder.layer.0.attention.self.value
...（12 层）
classifier.weight                              # 分类头
classifier.bias
```

---

### 3. `tokenizer_config.json` - 分词器配置

定义如何将文本转换为模型可以理解的数字。

```json
{
  "tokenizer_class": "BertTokenizer",      // 分词器类型
  "do_lower_case": false,                  // 不转小写（中文）
  "model_max_length": 512,                 // 最大长度
  "cls_token": "[CLS]",                    // 句子开始标记
  "sep_token": "[SEP]",                    // 句子结束标记
  "pad_token": "[PAD]",                    // 填充标记
  "mask_token": "[MASK]",                  // 掩码标记
  "unk_token": "[UNK]"                     // 未知词标记
}
```

**特殊标记说明：**
- `[CLS]`: 句子开头，用于分类任务
- `[SEP]`: 句子分隔符
- `[PAD]`: 填充到相同长度
- `[MASK]`: 掩码语言模型训练用
- `[UNK]`: 未知词（不在词汇表中）

---

### 4. `tokenizer.json` - 词汇表和分词规则

包含完整的词汇表和分词算法。

**内容：**
- 21,128 个中文词汇
- WordPiece 分词算法
- 词到 ID 的映射

**示例：**
```json
{
  "vocab": {
    "[PAD]": 0,
    "[UNK]": 100,
    "[CLS]": 101,
    "[SEP]": 102,
    "的": 103,
    "是": 104,
    ...
  }
}
```

---

## 🧠 模型的内部结构

### 完整的模型架构

```
输入文本: "这个产品很好"
    ↓
┌─────────────────────────────────────┐
│  1. Tokenizer (分词器)               │
│     "这个产品很好" → [101, 6821, ...]│
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  2. Embedding Layer (嵌入层)        │
│     Token IDs → 768维向量           │
│     + Position Embeddings           │
│     + Token Type Embeddings         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  3. BERT Encoder (12层)             │
│     ┌─────────────────────────┐    │
│     │ Layer 1: Self-Attention │    │
│     │          + Feed Forward │    │
│     └─────────────────────────┘    │
│     ┌─────────────────────────┐    │
│     │ Layer 2: Self-Attention │    │
│     │          + Feed Forward │    │
│     └─────────────────────────┘    │
│     ...                             │
│     ┌─────────────────────────┐    │
│     │ Layer 12: Self-Attention│    │
│     │           + Feed Forward│    │
│     └─────────────────────────┘    │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  4. Pooler (池化层)                  │
│     取 [CLS] 位置的向量              │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  5. Classifier (分类头)              │
│     768维 → 2维 (正面/负面)          │
└─────────────────────────────────────┘
    ↓
输出: [0.05, 0.95] → 正面 (95%)
```

---

## 🔢 模型的数学表示

### 1. 输入处理
```
输入文本 → Token IDs
"这个产品很好" → [101, 6821, 702, 772, 1501, 2523, 1962, 102]
```

### 2. 嵌入层
```python
# 词嵌入
word_embeddings = Embedding(vocab_size=21128, hidden_size=768)

# 位置编码
position_embeddings = Embedding(max_position=512, hidden_size=768)

# 组合
embeddings = word_embeddings + position_embeddings
# 形状: [batch_size, seq_length, 768]
```

### 3. Transformer 层
```python
# 每一层包含：
class TransformerLayer:
    def forward(x):
        # 1. 多头自注意力
        attention_output = MultiHeadAttention(x)
        x = LayerNorm(x + attention_output)
        
        # 2. 前馈网络
        ffn_output = FeedForward(x)
        x = LayerNorm(x + ffn_output)
        
        return x
```

### 4. 分类头
```python
# 取 [CLS] 位置的输出
cls_output = encoder_output[:, 0, :]  # [batch_size, 768]

# 线性分类
logits = Linear(768, 2)(cls_output)   # [batch_size, 2]

# Softmax 得到概率
probs = Softmax(logits)               # [0.05, 0.95]
```

---

## 💾 模型文件大小分析

| 文件 | 大小 | 占比 | 说明 |
|------|------|------|------|
| `model.safetensors` | ~412 MB | 99% | 模型权重 |
| `tokenizer.json` | ~2 MB | <1% | 词汇表 |
| `config.json` | ~1 KB | <0.1% | 配置 |
| `tokenizer_config.json` | ~1 KB | <0.1% | 分词器配置 |
| **总计** | **~414 MB** | 100% | |

---

## 🔄 模型的使用流程

### 加载模型
```python
from transformers import AutoModelForSequenceClassification, AutoTokenizer

# 加载模型和分词器
model = AutoModelForSequenceClassification.from_pretrained("./my_sentiment_model")
tokenizer = AutoTokenizer.from_pretrained("./my_sentiment_model")

# 模型会自动读取：
# 1. config.json → 构建模型架构
# 2. model.safetensors → 加载权重
# 3. tokenizer.json → 加载分词器
```

### 推理过程
```python
# 1. 文本 → Token IDs
text = "这个产品很好"
inputs = tokenizer(text, return_tensors="pt")
# inputs = {
#     'input_ids': tensor([[101, 6821, 702, 772, 1501, 2523, 1962, 102]]),
#     'attention_mask': tensor([[1, 1, 1, 1, 1, 1, 1, 1]])
# }

# 2. 前向传播
outputs = model(**inputs)
# outputs.logits = tensor([[-2.94, 2.94]])  # 原始分数

# 3. 计算概率
import torch.nn.functional as F
probs = F.softmax(outputs.logits, dim=-1)
# probs = tensor([[0.05, 0.95]])  # [负面, 正面]

# 4. 预测结果
prediction = torch.argmax(probs, dim=-1)
# prediction = 1 (正面)
```

---

## 📊 模型参数详细分解

### BERT-base-chinese 参数分布

```
总参数: 102,269,186 (约 102M)

1. 词嵌入层: 16,262,144
   - word_embeddings: 21,128 × 768 = 16,226,304
   - position_embeddings: 512 × 768 = 393,216
   - token_type_embeddings: 2 × 768 = 1,536
   - LayerNorm: 768 × 2 = 1,536

2. 12 个 Transformer 层: 85,054,464
   每层包含:
   - Self-Attention: 2,362,368
     * Query: 768 × 768 = 589,824
     * Key: 768 × 768 = 589,824
     * Value: 768 × 768 = 589,824
     * Output: 768 × 768 = 589,824
   - Feed-Forward: 4,722,432
     * Intermediate: 768 × 3072 = 2,359,296
     * Output: 3072 × 768 = 2,359,296
   - LayerNorm: 3,072
   
   每层总计: 7,087,872
   12 层: 7,087,872 × 12 = 85,054,464

3. Pooler 层: 590,592
   - Dense: 768 × 768 = 589,824
   - Bias: 768

4. 分类头: 1,538
   - Weight: 768 × 2 = 1,536
   - Bias: 2
```

---

## 🎯 模型的实际应用

### 1. 直接使用（Pipeline）
```python
from transformers import pipeline

classifier = pipeline("text-classification", model="./my_sentiment_model")
result = classifier("这个产品很棒！")
# [{'label': 'LABEL_1', 'score': 0.9468}]
```

### 2. 批量预测
```python
texts = [
    "质量很好，推荐购买",
    "太差了，不值这个价",
    "还可以，一般般"
]

results = classifier(texts)
for text, result in zip(texts, results):
    print(f"{text} → {result['label']} ({result['score']:.2%})")
```

### 3. 集成到应用
```python
class SentimentAnalyzer:
    def __init__(self, model_path):
        self.model = AutoModelForSequenceClassification.from_pretrained(model_path)
        self.tokenizer = AutoTokenizer.from_pretrained(model_path)
    
    def predict(self, text):
        inputs = self.tokenizer(text, return_tensors="pt")
        outputs = self.model(**inputs)
        probs = F.softmax(outputs.logits, dim=-1)
        return {
            'positive': probs[0][1].item(),
            'negative': probs[0][0].item()
        }

# 使用
analyzer = SentimentAnalyzer("./my_sentiment_model")
result = analyzer.predict("这个产品很好")
print(f"正面: {result['positive']:.2%}, 负面: {result['negative']:.2%}")
```

---

## 🔍 模型的可视化

### 模型结构图
```
BertForSequenceClassification(
  (bert): BertModel(
    (embeddings): BertEmbeddings(
      (word_embeddings): Embedding(21128, 768)
      (position_embeddings): Embedding(512, 768)
      (token_type_embeddings): Embedding(2, 768)
      (LayerNorm): LayerNorm((768,))
      (dropout): Dropout(p=0.1)
    )
    (encoder): BertEncoder(
      (layer): ModuleList(
        (0-11): 12 x BertLayer(
          (attention): BertAttention(
            (self): BertSelfAttention(
              (query): Linear(768, 768)
              (key): Linear(768, 768)
              (value): Linear(768, 768)
              (dropout): Dropout(p=0.1)
            )
            (output): BertSelfOutput(
              (dense): Linear(768, 768)
              (LayerNorm): LayerNorm((768,))
              (dropout): Dropout(p=0.1)
            )
          )
          (intermediate): BertIntermediate(
            (dense): Linear(768, 3072)
          )
          (output): BertOutput(
            (dense): Linear(3072, 768)
            (LayerNorm): LayerNorm((768,))
            (dropout): Dropout(p=0.1)
          )
        )
      )
    )
    (pooler): BertPooler(
      (dense): Linear(768, 768)
      (activation): Tanh()
    )
  )
  (dropout): Dropout(p=0.1)
  (classifier): Linear(768, 2)
)
```

---

## 📦 模型的分发和部署

### 1. 本地使用
```python
model = AutoModelForSequenceClassification.from_pretrained("./my_sentiment_model")
```

### 2. 上传到 Hugging Face Hub
```python
from huggingface_hub import HfApi

api = HfApi()
api.upload_folder(
    folder_path="./my_sentiment_model",
    repo_id="your-username/sentiment-model",
    repo_type="model"
)
```

### 3. 从 Hub 下载使用
```python
model = AutoModelForSequenceClassification.from_pretrained(
    "your-username/sentiment-model"
)
```

### 4. 转换为 ONNX（加速推理）
```python
from transformers.onnx import export

export(
    preprocessor=tokenizer,
    model=model,
    config=model.config,
    opset=14,
    output="model.onnx"
)
```

---

## 🎓 总结

### 模型的本质
训练后的模型实际上是：
1. **架构定义**（config.json）- 模型的"骨架"
2. **权重参数**（model.safetensors）- 模型的"知识"
3. **分词器**（tokenizer.json）- 文本处理工具

### 模型的形式
- **物理形式**: 一组文件（约 414 MB）
- **逻辑形式**: 一个神经网络（102M 参数）
- **功能形式**: 一个文本分类器（输入文本 → 输出类别）

### 模型的价值
- 包含了从数据中学到的**知识**
- 可以对新文本进行**预测**
- 可以**迁移**到类似任务
- 可以**分享**给其他人使用

**简单来说：训练后的模型就是一个"智能函数"，输入文本，输出分类结果！** 🎯
